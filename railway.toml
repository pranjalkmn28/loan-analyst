[build]
builder = "nixpacks"

[deploy]
startCommand = "python startup.py && gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 wsgi:application"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3